import { SearchParams } from '../api/api.service.types';

export const PRESETS_PREFIX_URL = 'content/v1/proxy/field-types';

export const DEFAULT_PRESETS_SEARCH_PARAMS: SearchParams = {
	skip: 0,
	limit: -1,
};
